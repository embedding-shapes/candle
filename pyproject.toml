[project]
name = "chived"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "accelerate>=1.10.1",
    "hf-transfer>=0.1.9",
    "httpx>=0.28.1",
    "huggingface-hub>=0.35.3",
    "kernels>=0.10.4",
    "torch>=2.9.0",
    "transformers[mistral-common]>=4.57.1",
    "triton>=3.5.0",
]
